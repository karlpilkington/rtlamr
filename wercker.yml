box: wercker/golang@1.1.2
platform : ubuntu@12.04
build:
    steps:
        - bundle-install
        - script:
            name: install libfftw3-dev
            code: sudo apt-get install libfftw3-dev
